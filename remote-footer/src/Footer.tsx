import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h2>Footer</h2>
      <p>This is a footer from remote-footer app</p>
    </footer>
  );
};

export default Footer;
